{
  "name": "nebula-genesis",
  "version": "1.2.3",
  "description": "Download and cache workflow for Notion databases.",
  "main": "/build",
  "bin": "/build/index.js",
  "scripts": {
    "dev": "npx tsc --build --watch",
    "build": "npx tsc --build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "notion",
    "jamstack",
    "sustainability"
  ],
  "author": "RIMAROK.com",
  "license": "ISC",
  "dependencies": {
    "@notionhq/client": "^2.2.9",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.7",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  }
}
