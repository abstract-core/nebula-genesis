{
  "name": "nebula-genesis",
  "version": "2.0.0",
  "description": "Fetch, cache and parse to MD, pages from Notion databases.",
  "main": "index.ts",
  "bin": {
    "genesis": "./build/index.js"
  },
  "scripts": {
    "start": "node build/index.js",
    "dev": "npx esbuild --watch --bundle --platform=node --outfile=build/index.js index.ts",
    "build": "npx esbuild --bundle --platform=node --outfile=build/index.js index.ts"
  },
  "keywords": [
    "notion",
    "jamstack",
    "sustainability"
  ],
  "author": "RIMAROK.com",
  "license": "ISC",
  "dependencies": {
    "@notionhq/client": "^2.2.9",
    "@types/sharp": "^0.31.1",
    "node-fetch": "^2.6.1",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/jest": "^29.5.7",
    "ts-jest": "^29.1.1",
    "typescript": "^5.1.6"
  }
}
